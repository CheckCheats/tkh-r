# theKing Hub

静态官网（GitHub Pages）。

- 作者：**theKing** · QQ **416946520**
- 白名单用户数 / 在册游戏 / 可展开功能简介
- 加载行默认隐藏：输入 Roblox 用户名校验加密白名单后才显示

## 更新清单与白名单

在 Core 仓库执行：

```bash
python www/_build_catalog.py
python www/_emit_gate.py
```

再把 `www/` 内容推到本仓库根目录。

## Pages

Settings → Pages → Deploy from branch `main` / root。
